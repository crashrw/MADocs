Preresiquites
pip install -r requirements.txt

Use "mkdocs serve" in the folder directory with docs to start a server
CTRL + c will stop the server

"mkdocs build" to build the static pages under the site folder